[project]
name = "gh-prune"
version = "0.1.1"
description = "CLI to prune GitHub workflow runs"
requires-python = ">=3.9"
readme= "README.md"
dependencies = ["typer"]

[project.scripts]
gh-prune = "gh_prune.cli:main"

[tool.setuptools.packages.find]
where = ["src"]
